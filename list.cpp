#include<iostream>
#include<list>
//using doubly linked list
//cannot randomly excess - O(n)

using namespace std;
int main(){
    list<int> l;
    l.push_back(1);
    l.pop_front(2);
    //push_back, pop_front, size
    //copying old list
    //list<int> n(l)
}